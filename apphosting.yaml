# Settings for Firebase App Hosting
# https://firebase.google.com/docs/app-hosting/configure

# The runtime to use for the server.
runtime: nodejs20

# The region to run the server in.
region: europe-west1

# Run config settings for the server.
runConfig:
  minInstances: 1
  maxInstances: 3
